# SyncYouTube — Modul-Karte

> **PFLICHT aktuell halten (JB-Dauerregel 20.07.2026):** Jede neue/entfernte Datei hier
> eintragen. Zweck: Zusammenhänge sehen → Ausfälle und redundante Dateien sofort erkennen.
>
> Angelegt 23.07.2026, weil der **Programm-Kanon** (`SyncDashTray/System/programm_kanon.py`)
> die Lücke gemeldet hat: SyncYouTube war das einzige Familien-Programm ohne Modul-Karte.
> Der Inhalt ist **nicht neu erfunden**, sondern aus dem vorhandenen `_ARCHITEKTUR.md` in
> die Familien-Form (Tabelle: Datei → Zweck → Verknüpfung) überführt. `_ARCHITEKTUR.md`
> bleibt als ausführliche Erzählung inkl. Schichtungs-Diagramm daneben stehen.

## Skripte

| Datei | Zweck | Verknüpfung |
|---|---|---|
| `huelle.py` | Programm-Hülle (Spec Stufe 2, JB-Go 05.08.): eigenes Fenster (pywebview) lädt die Oberfläche vom lokalen Server; startet den Server selbst, falls er aus ist. Bettet das VLC-Video in ein WinForms-Panel ein (`/api/vlc` Befehl `fenster`: Anmeldung mit PID, vor jedem VLC-Start neu, Abmeldung beim Schließen; in derselben Anfrage pausiert der Server ein Video, das gerade in IHR Panel spielt — Musik läuft weiter, JB 24.09.; ein Jellyfin-Film meldet dabei seine Stelle an Jellyfin, ohne „gesehen“, 25.09.). Port aus der `config.json` neben `huelle.py` (`adresse()`, Rückfall 8776; F25), die Fehlermeldung nennt `SyncYouTube.bat` | Start über `SyncYouTube-Fenster.bat`; setzt vor jedem Fenster die Kennung `JBK.SyncYouTube` (`windows_kennung`); ruft `youtube_app.py` als Subprozess; js_api zeigt der Seite NUR Methoden (`video_rect`, `video_melden`) — öffentliche Daten ließen pywebview in das .NET-Geflecht laufen (Hänger 07.08./24.09.); Wächter `tests/test_huelle.py` |
| `youtube_app.py` | Das Herzstück: Warteschlange + Worker, HTTP-Server (Port 8776), Download-Logik (yt-dlp), „Schon-geladen"-DB, Bibliothek, Playlists, Abos, Tray, `main()`. Löschen nie endgültig (25.09.2026, Gesamtprüfung S5/S6): Papierkorb, sonst (auf Wechseldatenträgern und Netzlaufwerken gleich) Ordner `_Papierkorb` im Download-Ordner (gemeldet in `yt_fehler.jsonl`); Spiegel-Sync verschiebt nach `_entfernt` im Ziel, Merkblatt `sync_kopien` je Zielordner; eine fremde gleichnamige Datei (andere Größe) wandert vor dem Kopieren ebenfalls nach `_entfernt` statt überschrieben zu werden, und jeder `_entfernt`-Ordner trägt eine leere `.nomedia` (Gruppe 6). Beide Ordner sind von Index, Einsortieren, Import und Suche ausgenommen. Untertitel eines Downloads mit URL-Schlüssel findet `untertitel_liste` nach dem Einsortieren über die Id seiner Datei (`_datei_videoid` + `_id_datei`, Gruppe 6). Vertrauen in Anfragen (25.09.2026, Gesamtprüfung Gruppe 3): vor jedem Riegel prüft der Handler den Host-Kopf (IP-Literal, localhost oder der eigene Rechnername allein bzw. mit Heimnetz-Endung `.local`, `.lan`, `.home.arpa`, `.fritz.box`, `.localdomain`; seit der Nachschärfung vom 25.09. nicht mehr unter fremder Domain) und den Origin-Kopf (fehlt, eigener Ursprung oder Browser-Erweiterung `moz-extension`/`chrome-extension`, dieselben Schemata wie CORS in `_cors`, das Alt-Edge-Schema ist raus (Gruppe 6); keine Content-Type-Pflicht, das Addon sendet ohne); jede Antwort trägt `frame-ancestors 'self' http://127.0.0.1:8765 http://localhost:8765` (Dashboard bettet `/?embed=1` ein). Ist die Fernsteuerung aus, lesen Geräte im WLAN „Fernsteuerung am PC ausgeschaltet.“ (Seiten als HTML, API als JSON) statt „nicht gekoppelt“. WLAN-Rechte (25.09.2026, JB-Entscheid 7a Punkt 2, S10): vor dem Routing prüft `Handler._lan_tor` jede Anfrage eines Nicht-Lokalen gegen `LAN_ERLAUBT` (abspielen, suchen, fernsteuern, Status und Bibliothek lesen, Downloads anstoßen; drei Routen mit Prüfer für einzelne Zweige: `/api/vlc`, `/api/biblio`, `/api/action` (dort nur „Ordner öffnen“; „Trotzdem laden“ geht seit Gruppe 6 wieder aus dem WLAN, weil `_vorhandene_sichern` die vorhandene Datei vorher rückholbar in den Papierkorb legt und ohne Sicherung nichts ersetzt; scheitert der neue Download, nennt der Fehlertext den Ort der Sicherung, `_sicherung_nennen`)); was in `NUR_PC` oder in keiner Tabelle steht, geht nur vom PC (403 mit `nur_pc`). „Lokal“ heißt überall `ist_loopback` (127.0.0.0/8 und ::1, auch als IPv4 in IPv6). Der Router selbst steht in `_get_routen`/`_post_routen`; beide vergleichen wie das Tor den Pfad ohne Anfrageteil, genau (der GET-Router seit Gruppe 6 auch; vorher teils mit Anfrageteil, teils nur den Anfang). Links aus dem WLAN (`/api/add`, `/api/kanal_info`, JB-Entscheid 7a Punkt 7): nur genau ein YouTube-Link (`ist_youtube_link`, Host exakt aus `YOUTUBE_HOSTS`, ohne Leerraum, Zeilentrenner, Steuer- oder Formatzeichen, Anmeldedaten oder fremden Port; Prüfer und `_add` zerlegen das Feld beide mit `link_zeilen`), kein Playlist-Ziel; vom PC jeder http(s)-Link, der nicht auf den PC selbst zeigt (`zeigt_auf_diesen_rechner`: jede Schreibweise von 127.0.0.0/8, 0.0.0.0, ::1, Namen unter .localhost, Namen nach der Auflösung und die eigenen Adressen). Kopplung und Code per Cookie (7a Punkt 1): ein gültiger Geräte-Token oder Code aus Adresse oder Kopf wird ein HttpOnly-Cookie (`syncyt_geraet` 400 Tage, gleitend: jede Seite aus `VERLAENGERN_SEITEN` setzt ein gültiges Token-Cookie mit voller Laufzeit neu; `syncyt_code` 30 Tage ab der Eingabe; SameSite=Strict, Path=/), eine Seite mit Token oder Code in der Adresse leitet ohne den Parameter um, Kopf und Adresse bleiben Rückfall, ein ungültiges Cookie löscht der Server in der Antwort; jede Antwort trägt `Referrer-Policy: no-referrer`; `/api/status` meldet `lokal`. Fernsteuerungs-Code (7a Punkt 3): neue Codes 10 Zeichen aus `CODE_ALPHABET` (ohne 0/O, 1/I/L), Knopf „Code erneuern“ am PC (`/api/code_erneuern`, nur PC), ein vorhandener Code bleibt bis zum Klick. Fernsteuerung aus sperrt Nicht-Lokale sofort (auch gekoppelte Geräte), Versuchsbremse je IP für Code und Geräte-Token (ab 10 Fehlversuchen 1 s, verdoppelnd bis 15 min; `_bremse_versuch` prüft und belegt in einem Schritt, gleichzeitig laufen höchstens so viele Vergleiche, wie bis zur nächsten Sperre frei sind, der Rest wartet kurz), `timeout` 30 s für das Lesen einer Anfrage (die Strom-Wege heben es nach dem Kopf auf, ein pausierter Film bricht nicht ab), POST-Körper höchstens 2 MB, abgewiesene POSTs schließen die Verbindung. `_id_tag_lesen` nimmt nur plausible Ids; `auffaellige_schluessel()` meldet Schlüssel mit Sonderzeichen beim Start, ändert nichts. Persistenz (25.09.2026, Gesamtprüfung Gruppe 4): `_json_speichern` schreibt über `familie.json_schreiben` (eigener tmp-Name je Faden, kurze Wiederholung bei Freigabekonflikt, OSError bei Scheitern), config.json nur unter EINER Sperre `_cfg_lock` (`_cfg_speichern`), eine defekte Datei wandert nach `<datei>.<Zeitstempel>.defekt` (nie über eine vorhandene); ein Sperr-Fehler beim Lesen (PermissionError, WinError 32/33) ist kein Defekt, `_json_laden` liest dann knapp 1 s lang erneut (Gruppe 6), `auto_neustart` steht in den Vorgaben. Der Worker überlebt ein gescheitertes Speichern (`_q_speichern_im_worker`), `_worker_start` zählt nur lebende Fäden, und der Ticker ersetzt tote Worker alle 5 s. Auflösen (`aufloesen`): Platzhalter sofort, der yt-dlp-Abruf wartet auf einen von zwei Plätzen (`_aufloese_plaetze`, F2; ein Abruf, der länger als 5 min auf seinem Platz sitzt, gilt als hängend und gibt den Platz an den nächsten Link ab, `queue_heilen` weckt die Wartenden); die 5-min-Heilung zählt erst ab dem Eintritt, und das Ergebnis ändert den Status nur, solange der Eintrag noch „prueft“ (F3). Schutzschalter gegen YouTube-Sperren (F4): jede erkannte Sperre (Download, Auflösen, Nebenweg) setzt über `youtube_sperre_vermerken` eine Pause von 30 min; die Nebenwege (Untertitel, Abo-Blick, Anreichern, Entdecken) laufen über `_nebenweg_abruf` (Sperre: gemeldet, kein Zweitversuch, Cookies bleiben; nur ein Cookie-Fehler bekommt einen Zweitversuch ohne Cookies), die Serien-Schleifen (Anreichern, Nachreichern, Abo-Prüfung, Entdecken samt jedem Seed) lesen `youtube_gesperrt()` vor jedem Abruf; was JB einzeln anstößt (Abo anlegen, Kanal-Rückfrage, Backkatalog, Untertitel), fragt auch in der Pause. Bekommt die Abo-Anlage keine Antwort, trägt das Abo `basis_offen`, und `_abo_heilen` holt die Baseline beim nächsten Puls nach, ohne zu laden. Bibliotheks-DB `_geladen` (F6): Einträge kommen und gehen nur unter `_io_lock`, Durchläufe über `_geladen_schnappschuss()`/`_geladen_schluessel()`, gespeichert wird über `_geladen_speichern()` (Text unter der Sperre, Schreiben außerhalb, ein älterer Stand überschreibt nie einen neueren). Keine Sperre über Ordnerlauf, Explorer-Start oder das Senden einer Antwort (F7): `/api/status` zieht unter `Q.lock` nur einen Schnappschuss, `/api/bibliothek` läuft ohne Sperre, Playlists/Abos werden unter `_io_lock` nur serialisiert, `_aufloesen_einreihen` fragt die Platte vor und schreibt Playlists nach der Sperre; in der Bibliothek suchen „Im Ordner zeigen“, „Extern abspielen“ und die Einzel-Löschung die Datei und starten Explorer, Player bzw. Papierkorb ohne Sperre (`_datei_rueckholbar_entfernen`), die Löschung nimmt danach unter `_io_lock` den Eintrag nur heraus, wenn er nicht derweil frisch geladen wurde. „Läuft schon“-Merker prüfen und setzen in einem Schritt (`_LaeuftSchon` für Technik, Metadaten, Einsortieren, Anreichern; `_zustand_starten` für den Geo-Test, dessen Knopf den Merker vor dem Faden setzt); `autotag_lauf` merkt sich Aufträge, die während eines Laufs kommen, und holt sie am Ende nach (F8; einen Voll-Lauf nur hinter einem Schlüssel-Durchgang, den Merker gibt es am Ende genau einmal frei) | importiert `familie`, `geo`, `oberflaeche`, `handy`, `windows_kennung`, `cookie_kopie`; jeder YoutubeDL entsteht in `_ydl` (Wächter `tests/test_cookies_wal.py`); **Einbahn-Regel:** importiert NIE `vpn` direkt |
| `filme.py` | Film-Fundament (`Doku/SYNC_FILME_SPEC.md`, JB-Go 05.08.): Jellyfin-Katalog (Renés Server) als lokaler Spiegel, Bild-Cache, TMDB/OMDb-Anreicherung, Reihen-Engine, Stream-URL für den VLC-Motor, Fortschritts-Queue; Zugangsdaten NUR im Keyring, Token verlässt den Server nie. Detailseite (F12, 25.09.2026): ein Ausfall von TMDB oder OMDb (Netzfehler, Antwort außer 200/404, OMDb-Tagesdeckel) markiert den Eintrag `unvollstaendig`, der nur eine Stunde hält; ein abgelehnter Schlüssel (401) ruht einen Tag, ein getauschter Schlüssel holt sofort nach (im Cache steht nur ein Fingerabdruck), und eine Quelle, die beim Nachholen nicht antwortet, behält ihre alten Felder; der OMDb-Tageszähler wird im selben `_json_aendern` gespeichert. `reihen()` eicht die Top-Kandidaten mit höchstens 8 TMDB-Versuchen je Aufruf (F13): nach Netz-, 401-, 429- oder Serverfehler ruht das Eichen eine Stunde, ein bei TMDB unbekannter Titel (404) einen Tag. Eine angekommene Fortschritts-Meldung reicht liegengebliebene nach (F19): höchstens alle 10 min, im Hintergrund, ein Nachreichen zur Zeit (auch gegen den Katalog-Abzug); eine Ausnahme steht als `nachreichen_fehler` in `filme_zustand.json`. Der Bild-Cache schreibt atomar (eigener Zwischenname, `os.replace`; F20). Snippet-Backen nur für Titel aus dem Spiegel, „läuft schon?“ unter `_snippet_lock` geprüft und gemerkt (F23) | von `youtube_app` gerufen (`/api/filme/*`, 6-h-Haken in `ticker_schleife`); importiert NIE zurück (Einbahn-Regel wie geo/vpn) |
| `profil_geraete.py` | Teilprojekt 3 (JB-Go 05.08.): Profile („Wer schaut?") + Geräte-Registry mit Pairing-Code-Fluss und je-Gerät-Token (einzeln widerrufbar, an Profil gebunden); `geraet_ok()` = Riegel-Kern für alle Nicht-localhost-Zugriffe (Token und Pairing-Code zeitkonstant verglichen, `hmac.compare_digest`, 25.09.2026); jeder Schreiber geht durch `_aendern` (Lesen-Ändern-Schreiben unter `familie.json_aendern` plus Modul-`RLock`, S8), Prüfen und Auflisten schreiben nie, eine unlesbare `profile.json` wird nie überschrieben; Pairing-Seite (`PAIRING_HTML`; speichert den Token nicht mehr, ein alter `ytdl_geraet_token` geht einmal an den Server und wird entfernt, ein schon gekoppeltes Gerät springt zurück auf `/`; kommt der Tab binnen 30 s wieder, gilt das als Schleife und das Gerät meldet sich an; scheitert das Anmelden, steht dort eine Meldung statt „undefined“, und die Seite fragt nicht im Takt nach) und die Seite „Fernsteuerung am PC ausgeschaltet“ (`FERNSTEUERUNG_AUS_HTML`). Bewusst nicht `profile.py` (stdlib-Schatten) | von `youtube_app` gerufen (`/api/geraet_*`, `/api/profile`, `/koppeln`, `_hat_zugriff`); importiert NIE zurück |
| `live_tv.py` | 📡 Live-TV (JB-Go 05.08.): lädt + parst die legale kodinerds-clean-M3U (öffentlich-rechtliche m3u8), 24-h-Cache; VLC spielt die Streams direkt. Nach einem gescheiterten Abruf ruht die Quelle eine Stunde (`letzter_versuch`, F18), der alte Cache trägt | von `youtube_app` gerufen (`/api/live`, `/api/live/play`); importiert NIE zurück |
| `geo.py` | Gestufte Geo-Umgehung (Header-Trick → eigene Proxys → Gratis-Proxys → VPN-Adapter); erkennt und parst Geo-Fehler **provider-unabhängig**; jeder Proxy steht nur einmal in der Kette (F21). VPN-Adapter (F22): vorher den Zustand fragen (NordVPN-Insights, `windscribe-cli status`, WireGuard-Dienst `WireGuardTunnel$<Name>` über `_dienst_da`, ctypes, nur lesend); ist der Nutzer schon verbunden, wird durch seinen Tunnel versucht und nichts getrennt, ist der Zustand unbekannt, wird nichts angefasst; Erfolg erst nach dem Rückgabecode, getrennt wird nur die eigene Verbindung | von `youtube_app` gerufen; ruft `vpn` |
| `vpn.py` | Ausschließlich NordVPN-Steuerung (CLI + Insights-Status) — NordVPN-Interna leben nur hier | nur von `geo.py` gerufen (Einbahn-Abhängigkeit seit 09.07.2026) |
| `oberflaeche.py` | Die gesamte PC-Oberfläche als ein HTML/CSS/JS-String; wird bei jedem Seitenaufruf heiß nachgeladen (`importlib.reload`). Läuft sie auf einem gekoppelten Gerät im WLAN (`/api/status` meldet `lokal: false`, 7a Punkt 1), schaltet `fernModusSetzen` den Fern-Modus: Klasse `fern` am `<body>` blendet alles mit `nur-pc` aus (Einstellungen, Abos, Geräte, Fernsteuerung), `menuFuerGeraet` filtert mit `nurPc()` markierte Menü-Einträge, eine trotzdem abgewiesene Aktion meldet sich einmal (`fetchHuelleSetzen`); Wiedergabe-Wahlen des Geräts (Qualität, Untertitel-Modus und -Stil, Tempo, Versatz) gehen nicht an den Server, Untertitel-Modus und -Stil merkt sich der Browser des Geräts (`fernWahl`, `ytdl_fern_wahl`) und sie schlagen dort den Standard des PCs. Selbst-Erneuerung bei neuem `ui_stand` (JB-Entscheid 7a Punkt 5, F10): spielt Musik (`#pl-el`) oder VLC, bleibt das Neuladen vorgemerkt; der Sekundentakt holt es in der nächsten Pause nach, am Titelende ersetzt es das Weiterschalten (`uiNeuLaden` in `plTitelEnde` und `vlcTick`). Crossfade und Automix starten bei vorgemerktem Stand keine Blende (`uebergangTick`), und eine schon hörbare Blende (`xfNext` spielt) hält das Neuladen auch am Titelende bis zur nächsten Pause auf. Kopfleiste und Player-Leiste spulen über dieselben Helfer `spulStand`/`spulenAuf`, auch am Gerät VLC (F11). Eine neue Playlist entsteht über `plAnlegen(name)`, das die id aus der Antwort des Servers nimmt (`/api/playlist` `create` antwortet mit `id`, F17) statt „die zuletzt gelistete“ zu raten. Gesperrter Browser-Speicher bricht das Skript nicht mehr ab: die Gerätewahl `plGeraet` und `uiNeuLaden` lesen und schreiben ihn in try (F27). Jellyfin-Ids, Geräte- und Profil-Ids und Trailer-Schlüssel gehen wie die Bibliotheks-Schlüssel nur als `data-arg` in Handler (`this.dataset.arg`), Ersatzbilder als `data-fb`/`data-fb1`/`data-fb2` (Gruppe 6; Wächter `tests/test_schluessel_im_handler.py`). Der M3U-Import misst den Körper vorher (höchstens 2 MB wie `MAX_KOERPER`) und meldet eine Abweisung des Servers mit ihrem Grund statt „Import ✓“ | von `youtube_app` ausgeliefert; enthält den `LAYOUT_KERN`-Inline-Block (Wächter `test_layout_kern` im Familien-Repo erzwingt Gleichheit mit `SyncDashTray/System/layout_kern.js`) |
| `handy.py` | Schlanke Touch-Oberfläche der Handy-Fernsteuerung (Route `/m`, opt-in mit Code); seit 23.09.2026 Sperrbildschirm-Steuerung (Titel/Cover/Vor/Zurück), solange das Handy selbst spielt. Der Titel-Schlüssel geht nur als `data-key` in den Handler, `esc` maskiert auch Anführungszeichen (25.09.2026, S3). Der Code lebt nur noch als HttpOnly-Cookie des Servers (7a Punkt 1); ein alter `ytdl_code` im localStorage geht einmal an den Server und wird entfernt; ohne Cookies gilt der Code nur, solange die Seite offen ist (`KOPF`, `mitCode`) | von `youtube_app` ausgeliefert; setzt `medien_session.MEDIEN_JS` ein |
| `medien_session.py` | Gemeinsamer Media-Session-Baustein (23.09.2026) für PC-Oberfläche und Handy-Seite: Knöpfe an-/abmelden, Metadaten, Zustand + Zeitleiste (geklemmt, Strom = gelöscht), `freigeben()` für abgelöste Medienelemente. Weiß NICHT, was „Weiter" bedeutet — das entscheidet die Seite | per Platzhalter `/*MEDIEN_SESSION_JS*/` in `oberflaeche.HTML` und `handy.HTML` eingesetzt (Rohvorlage `_HTML_ROH`); heiß nachgeladen vor beiden Seiten und im `ui_stand` mitgezählt; Wächter `tests/test_medientasten_verhalten.py` + `tests/test_medientasten_randfaelle.py` (führen das JS mit deno aus) |
| `medien_smtc.py` | Windows-Anmeldung des VLC-Motors (23.09.2026, JB-Go „pywinrt im Server"): verstecktes Fenster + `SystemMediaTransportControls` — Titel/Cover/Zeitleiste im Windows-Overlay, Knöpfe/Medientasten steuern VLC; Weiter/Zurück reicht es als `taste`-Zähler an die Seite | von `youtube_app` gerufen (`/api/vlc`: `cmd:"medien"`, Felder `smtc`/`taste` in jeder Antwort); Pakete `winrt-*` 3.2.1; fehlt winrt, läuft alles ohne Anmeldung weiter; Wächter `tests/test_medien_smtc.py` |
| `windows_kennung.py` | Windows-Kennung (JB 24.09.2026: „Kennung + Startmenü-Eintrag"): `setze_kennung()` meldet den Prozess als `JBK.SyncYouTube` — ohne sie ordnete Windows den pythonw-Server der Verknüpfung „IDLE (Python 3.14)" zu (Befund 23.09.); `startmenue_eintrag()` legt die Verknüpfung „SyncYouTube" im Startmenü des Nutzers mit derselben Kennung an bzw. zieht sie nach (Ziel wie `SyncYouTube.bat` bzw. die exe; EINE stabile Regel seit 25.09.: das Ziel eines eigenen Eintrags wechselt nur, wenn das bisherige nicht mehr startet — sonst „behalten“; idempotent; eine fremde Verknüpfung gleichen Namens ohne diese Kennung bleibt unberührt; Schreiben über Zwischendatei + `os.replace`). Nur ctypes-COM (IShellLinkW/IPersistFile/IPropertyStore), kein pywin32 | `youtube_app.main()` ganz vorn (`setze_kennung`) und hinter dem Einzel-Instanz-Riegel im Hintergrundfaden (`startmenue_eintrag`, nie im Testmodus); `huelle.main()` vor jedem Fenster (`setze_kennung`); exe über `hiddenimports` der spec; Wächter `tests/test_windows_kennung.py` (schreibt nur in tmp_path, liest per PowerShell zurück); die Startmenü-Wache steht in `tests/conftest.py` |
| `cookie_kopie.py` | Firefox-Cookies samt WAL für yt-dlp (24.09.2026, Lehre aus SyncFindus `fremde_datenbank.lese_kopie`): yt-dlp 2026.08.19 kopiert nur `cookies.sqlite`, nicht das `-wal` — frische Anmelde-Cookies fehlten. `firefox_profil(auswahl)` kopiert Hauptdatei + `-wal` NUR als Datei (Firefox darf laufen, auch mit exklusiver Sperre; WAL-Kopf-Vergleich gegen einen Neubeginn), zieht per Backup-API EINE vollständige `cookies.sqlite` (Rollback-Modus) in einen frischen Temp-Ordner und gibt dessen Pfad als Profil; danach ist der Ordner weg. Jeder Fehler ⇒ None ⇒ der heutige Weg. Liest nie Cookie-Werte | nur von `youtube_app._ydl` gerufen (bei `cookiesfrombrowser == ("firefox",)`); `WURZELN` injizierbar — `tests/conftest.py` setzt es in JEDEM Test auf einen Ort ohne Profil (nie JBs echtes Firefox-Profil); exe über `hiddenimports` der spec; Wächter `tests/test_cookies_wal.py` (echte SQLite mit WAL in tmp_path, echter yt-dlp-Extraktor, kein Netz) |
| `update.py` | Selbst-Aktualisierung: prüft `releases/latest`, lädt die neue Fassung, tauscht sie aus. **Vorgabe seit 08.09.2026: AN** (`STANDARD_CONFIG["auto_update"]`); der Tausch läuft nur im Leerlauf (derselbe `_code_leerlauf` wie beim Selbst-Neustart: kein Download, keine Auflösung, keine Wiedergabe im Browser oder am VLC; F14), weil er den Prozess hart beendet. Signatur Pflicht (JB-Entscheid 7a Punkt 6, S9, 25.09.2026): ohne `.sha256` im Release kein Tausch; die neue exe liegt erst unter dem Zwischennamen `.tmp`, `signatur_pruefen` verlangt dort eine gültige Authenticode-Signatur (WinVerifyTrust über ctypes, samt Sperrlisten: `authenticode_online`) mit demselben Inhaber und Aussteller wie die laufende exe (lokal gelesen: `authenticode_offline`, und zwar schon VOR dem Laden über `laufende_signatur`: ist die laufende exe nicht gültig signiert, wird nichts geladen); `signaturblock_pruefen` verlangt vorher den Signaturblock so, wie signtool ihn schreibt (ein PKCS#7-Eintrag bis zum Dateiende, dahinter nur die Füllung auf 8 Bytes); ein gescheiterter Sperrlisten-Abruf wird als solcher gemeldet und bleibt ein Nein; erst dann wandert sie an den Zielnamen, eine verworfene bleibt als `SyncYouTube_neu.exe.verworfen` liegen. Der Neustart nach dem Tausch behält die Startargumente (F24) | von `youtube_app._update_hintergrund` täglich gerufen; Wächter `tests/test_vorgaben.py`, `tests/test_update_signatur.py` (Attrappen der WinVerifyTrust-Antwort, dazu der echte Aufruf lokal am signierten Python); die conftest sperrt `authenticode_online` (Netz); **MERKE:** Setup-Installationen nie auf die onefile-Form zurückfallen lassen |
| `tools/medien_probe.py` | Werkstatt (23.09.2026): misst Medientasten und Windows-Medien-Overlay über Windows' EIGENE Sitzungsliste (GSMTC per PowerShell-WinRT) — welche App ist „aktuell" (bekommt die Tasten), Titel/Interpret/Album/Zeitleiste/Knöpfe; drückt Medientasten per SendInput (nur mit `--nur-wenn`, wenn die eigene Sitzung aktuell ist) und Overlay-Knöpfe gezielt an EINE Sitzung | von Hand bei Prüfungen; nie im Auslieferungsweg. Vorsicht: Spotify pausiert bei Play/Pause mit, solange es spielt |
| `build_release.py` | Werkstatt (08.09.2026): EIN Weg für die exe — PyInstaller (`SyncYouTube.spec`), seit 24.09. mit fail-closed Erzeugnis-Prüfung (stehen die winrt-Erweiterungen aus den Importen von `medien_smtc.py` und `winrt/msvcp140.dll` in `build/SyncYouTube/PKG-00.toc`?), dann signieren, Prüfsumme, nach oben kopieren. Ohne gesteckten Signatur-Token baut es gar nicht erst, und scheitert das Signieren (`signiere_pflicht`), entsteht weder Prüfsumme noch Kopie (JB-Entscheid 7a Punkt 6, 25.09.2026: das Selbst-Update tauscht nur signierte exe) | von Hand vor einem Release; ruft `SyncDashTray/System/signieren.py`. Wächter: `tests/test_build_release.py` (auch die Schnittstelle zu `signieren.py` der Familie: genutzte Namen und Schlüsselwörter aus dem Syntaxbaum; ohne Familie übersprungen) |
| `tools/quellstart_paket.py` | Werkstatt: baut `SyncYouTube-Quellstart.zip` (signiertes python.org-Embeddable in der Fassung des Bau-Pythons + gepinnte Pakete samt pywinrt + Laufzeit-Dateien ohne Werkstatt + `bin/` + LICENSE/LIZENZEN.md/README.md) für Smart-App-Control-Nutzer (Pete-Fall 07.08.; Umbau 24.09., weil das v.1.2.6-ZIP nicht lauffähig war). Baut in einem frischen Ordner, Pflicht-Rauchtest vor dem Zippen | von Hand vor einem Release; Ergebnis nach `dist_exe/`, mit `--ausgabe ORDNER` woandershin (Probebau). Wächter: `tests/test_quellstart_paket.py` |
| `fernbedienung.py` | Fake-Fernbedienung unter `/fernbedienung`: kleines Fenster mit D-Pad, sendet Tastennamen per BroadcastChannel an den Fernsehmodus (JB-Wunsch 07.08.) | `youtube_app.py` (Route, heiß nachgeladen: steht wie `oberflaeche` und `handy` in der EINEN Tabelle `HEISSE_SEITEN`, die Router, Selbst-Neustart und `ui_stand` lesen; eine Änderung startet den Server nicht neu); Gegenstück in `oberflaeche.py`: `fbEmpfangVerkabeln`/`fernbedienungOeffnen` |
| `familie.py` | gemeinsamer Kern der Familie (Pfade/json/status) — byte-gleiche Kopie | Wächter `test_familie_kern` im Familien-Repo erzwingt Gleichheit; Verteilung über `vendor_kern.py` |
| `layout_kern.js` | gemeinsamer Layout-Mathe-Kern (Snapping/Überlappung/Viewport-Einpassung, DOM-frei) — vendorte Kopie | Master liegt in `SyncDashTray/System/layout_kern.js` |

## Unterordner

| Ordner | Zweck |
|---|---|
| `tests/` | Verhaltens-Sicherheitsnetz (kein Netz, Daten je Test in `tmp_path`). Prüfweg ist pytest: `python -m pytest tests` aus `System/`; `python tests/test_youtube.py` übergibt an pytest und fährt alle Tests dieser Datei (Gesamtprüfung Gruppe 7, geprüft in `test_wachen.py`). `conftest.py` (24./25.09.2026): Wachen für JEDEN Test — die Firefox-Profilsuche von `cookie_kopie` UND yt-dlps eigene (`_firefox_browser_dirs`) zeigen auf einen Ort ohne Profil; `windows_kennung.startmenue_ordner` ist gesperrt und schlägt laut an (auch im Hintergrundfaden, Meldung am Testende); `youtube_app` wird früh importiert (sein `filme.einrichten(DATEN_DIR)` beim Import lief sonst nach dem `einrichten(tmp_path)` eines Tests). Daten-Wache (25.09.2026, Gesamtprüfung Y0): alle Datenpfade der App (am Wert gefunden, nur `SCRIPT_DIR`/`BIN_DIR` bleiben), `profil_geraete` und `live_tv` liegen je Test in `tmp_path`, die beim Import geladenen Zustände (Q, CFG, Bibliothek, Playlists, Abos …) kommen je Test frisch; ein Audit-Hook lässt jedes Schreiben in den Programmordner laut scheitern, dazu Netzsperren für `live_tv`, `geo`, `vpn` und `update`. Der Direkt-Lauf von `test_youtube.py` geht über pytest und hat damit dieselbe Wache (vorher ein eigener Runner ohne Wache, der 59 von 213 Tests sah). Geprüft in `test_wachen.py`. `test_oberflaeche_syntax.py` (Gesamtprüfung O0): alle `<script>`-Blöcke der Oberfläche einzeln und zusammen per deno übersetzt, nie ausgeführt (fängt Syntaxfehler und doppelte `let`/`const` über Blöcke hinweg); fehlt deno, sichtbar übersprungen. `test_loeschen_und_pfade.py` (Gesamtprüfung Gruppe 2): Abo-Index, Cover, Untertitel, Lösch-Rückfall, Playlist-Sync, Live-TV und WireGuard mit `..\x`, absolutem, UNC- und gültigem Wert; fängt jeden Datei-Zugriff auf UNC-Pfade ab, bevor Windows ihn sieht `test_zugang_und_vertrauen.py` (Gesamtprüfung Gruppe 3): echter Handler samt echtem Riegel (ohne Ersatz) mit LAN-Adresse, Code, Geräte-Token, fremdem Host, fremdem Origin und Addon-Ursprung; Versuchsbremse mit steuerbarer Uhr und parallelen Stößen hinter einer Schranke; am echten ThreadingHTTPServer auf 127.0.0.1: Zeitlimit beim Lesen, Strom mit Pause über alle drei Strom-Wege, Keep-Alive-Nachbau und eine im Körper eingeschmuggelte zweite Anfrage. `test_zugang_seiten_js.py` (7a Punkt 1): Kopplungsseite, Handy-Seite und der Fern-Modus der Oberfläche als echtes Seiten-JavaScript per deno. `test_wlan_rechte.py` (Gesamtprüfung Gruppe 5, JB-Entscheide 7a): liest die Routen aus dem Syntaxbaum des Routers und schickt jede mit LAN-Adresse und gültigem Code (und vom PC) durch den echten Handler, der Router selbst ist dabei ein Zeuge; dazu gemischte Routen, Loopback-Schreibweisen und echte Routen ohne Zeugen. Verhaltensanker `JB_NUR_PC`: je Kategorie des JB-Entscheids eine feste Route durch den echten Router mit einem Fühler an der Funktion, die die Wirkung hätte (aus dem WLAN stumm, vom PC schlägt er an); rutscht eine gefährliche Route nach `LAN_ERLAUBT`, wird er rot. `test_schluessel_im_handler.py` (S3): rendert Kachel, Listenzeile, Dubletten, Eigenschaften, Transkript-Suche und die Handy-Liste per deno mit einem Schlüssel aus ' " < und prüft das HTML mit einem Parser. `test_oberflaeche_laden.py` (F9, F10): `laden()`/`configFuellen()` und das Neuladen bei Pause oder Titelende als echtes Seiten-JavaScript, auch mit Crossfade und Automix über den echten Player-Kern. Gesamtprüfung Gruppe 6 (25.09.2026): `test_update_signatur.py` (S9, F24; Attrappen der WinVerifyTrust-Antwort, der echte ctypes-Aufruf lokal am signierten Python und die Aufbau-Prüfung des Signaturblocks an Kopien davon), `test_oberflaeche_nebenfehler.py` (F11, F17, F26, F27, M3U-Import; deno), `test_filme_nebenfehler.py` (F12, F13, F19, F20, F23), `test_app_nebenfehler.py` (F14, F17, F21, F22, deno-Umgebung). `test_nebenlaeufigkeit_und_persistenz.py` (Gesamtprüfung Gruppe 4): echte Fäden und echte Dateien in tmp_path, ein echter Leser hält die Zieldatei offen; Schreiben mit Wiederholung, CFG-Sperre, Rettungskopie, Vorgaben, die echte Worker-Schleife mit einmal scheiterndem Speichern, das echte Auflösen mit einer yt-dlp-Attrappe hinter einem Tor, die drei Nebenwege und die Serien-Schleifen mit Bot-, Cookie- und Netzfehler, drei Fäden auf einer Bibliothek mit 3.000 Einträgen, ein langsamer Client und ein angehaltener Ordnerlauf gegen Worker und Downloads, Auto-Tag mit Nachhol-Auftrag und angehaltenem Lauf-Ende, zwei hängende Auflösungen gegen neue Links (stellbare Uhr), Einzelaktionen und Abo-Anlage in der Sperr-Pause, ein eingeschleuster langsamer Serialisierer gegen die CFG-Sperre (ohne `_cfg_lock` rot), acht gleichzeitige Anstöße je Hintergrundlauf in der ungünstigsten Verzahnung (threading.settrace, Haltepunkte am Syntaxbaum gefunden und gezählt). `test_toter_code.py` (Gesamtprüfung Gruppe 7, Abschnitt 5): die entfernte Route `POST /api/importieren` und der entfernte Zweig `art:'bulk'` in `_biblio` (er trug eine Lösch-Operation ohne Oberfläche) je durch den echten Handler; Wächter über das erzeugte Skript der Oberfläche: jede Top-Level-Funktion wird irgendwo genannt (Seitentext ohne Kommentare oder ein anderer Auslieferungs-Teil wie Hülle, Handy-Seite, Server, Browser-Erweiterung), und jede per `getElementById('…')` geholte ID entsteht auch irgendwo (Markup, `.id='…'` oder der Auswahl-Helfer `sel`), je mit Gegenprobe; dazu die entfernten Klassen ohne Element. `test_doppelungen.py` (Gruppe 7): was der echte Router pro Anfrage neu lädt, ist genau die Liste, die den Selbst-Neustart nicht auslöst (vorher fehlte `fernbedienung.py`); was `ui_stand` zählt; der JS-Fehler-Rekorder schreibt über denselben Schreiber wie der Fehlerkanal (`_zeile_anhaengen`: Sperre `_fehler_lock`, Deckel 200 KB), geprüft mit gehaltener Sperre; Kanal-Link-Regel (`_KANAL_UNTERSEITEN`, `_KANAL_WURZEL`) und Musikmuster (`_ist_musik_muster`) stehen je einmal: beide Leser urteilen gleich, eine Änderung wirkt auf beide. `test_protokoll.py` (Gruppe 7, Plan Abschnitt 6 Punkt 1): `_sag` schreibt auch ohne Konsole ins Protokoll, Deckel 1 MB mit drei Vorgängern, `_smtc_log`/`_kennung_log` mit Stufe WARNING und weiter im Fehlerkanal, `main()` richtet das Protokoll vor allem anderen ein, die Datei ist gitignored. `test_oberflaeche_einheitlich.py` (JB-Entscheid 7a Punkt 8): die gedämpfte Textfarbe steht nur noch als Variable `--gedaempft` (Tag-Modus: eigene Fassung), nie mehr inline; das Bibliotheks-Menü baut über `kontextMenuBauen` und klappt „Zu Playlist“ und „Ausschnitte“ rechts aus wie jeder Rechtsklick (deno, kleines Menü-DOM) |
| `browser-addon/` | Universelle Erweiterung für Firefox/Chrome/Edge aus EINEM Code (`build.py`); AMO-signiert, hält sich über `updates.json` selbst aktuell |
| `firefox-addon/` | Firefox-spezifische Altfassung/Signier-Artefakte |
| `abo_index/` | Index-Dateien der Kanal-/Playlist-Abos (Backkatalog, RSS-Puls) |
| `lizenzen/` | Lizenztexte fremder Bestandteile, deren Paket keinen mitbringt (24.09.2026: `pywinrt_LICENSE.txt`, MIT, weil pywinrts dist-info keine LICENSE enthält); geht mit ins Quellstart-Paket, Übersicht in `../LIZENZEN.md` |
| `bin/` | mitgelieferte Werkzeuge — **darin Deno**: yt-dlp braucht seit 2026 eine JS-Runtime für YouTubes n-Challenge, sonst „No video formats found"; die App setzt `DENO_NO_UPDATE_CHECK=1` (eine eigene Wahl in der Umgebung bleibt), damit deno nicht täglich im Netz nach Versionen fragt |
| `build/`, `dist/`, `dist_exe/`, `build_tmp/` | PyInstaller-Bauwerk und Ergebnisse (nicht committen) |

## Daten (nicht committen, enthalten JBs Daten)

`config.json` (Einstellungen: Port, Cookies-Browser, Geo, Unterordner) ·
`warteschlange.json` (offene Downloads, überlebt Neustarts, Resume via `.part`) ·
`geladen_log.json` (die Bibliothek = fertige Downloads) · `playlists.json` ·
`abos.json` · `lyrics_cache.json` (LRCLIB-Karaoke) ·
`yt_status.json` (**read-only fürs Dashboard**: Zähler + letzte Datei) ·
`yt_protokoll.log` (+ `.1` bis `.3`, 25.09.2026): was `_sag` meldet, auch unter pythonw und in der exe, wo es keine Konsole gibt; höchstens 1 MB je Datei (`logging`, `RotatingFileHandler`, eingerichtet als Erstes in `main()`) ·
`yt_fehler.jsonl`/`js_fehler.jsonl` (Fehlerkanal und Rekorder der Oberfläche, je höchstens 200 KB)

## Doku daneben

`_ARCHITEKTUR.md` (Schichtungs-Diagramm + Aufräum-Fahrplan) ·
`NAECHSTE_SESSION.md` (Übergabe innerhalb dieses Repos) ·
`SyncYouTube.spec` (PyInstaller)
