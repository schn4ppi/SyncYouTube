# SyncYouTube — Modul-Karte

> **PFLICHT aktuell halten (JB-Dauerregel 20.07.2026):** Jede neue/entfernte Datei hier
> eintragen. Zweck: Zusammenhänge sehen → Ausfälle und redundante Dateien sofort erkennen.
>
> Angelegt 23.07.2026, weil der **Programm-Kanon** (`SyncDashTray/System/programm_kanon.py`)
> die Lücke gemeldet hat: SyncYouTube war das einzige Familien-Programm ohne Modul-Karte.
> Der Inhalt ist **nicht neu erfunden**, sondern aus dem vorhandenen `_ARCHITEKTUR.md` in
> die Familien-Form (Tabelle: Datei → Zweck → Verknüpfung) überführt. `_ARCHITEKTUR.md`
> bleibt als ausführliche Erzählung inkl. Schichtungs-Diagramm daneben stehen.

## Skripte

| Datei | Zweck | Verknüpfung |
|---|---|---|
| `huelle.py` | Programm-Hülle (Spec Stufe 2, JB-Go 05.08.): eigenes Fenster (pywebview) lädt die Oberfläche vom lokalen Server; startet den Server selbst, falls er aus ist. Bettet das VLC-Video in ein WinForms-Panel ein (`/api/vlc` Befehl `fenster`: Anmeldung mit PID, vor jedem VLC-Start neu, Abmeldung beim Schließen; in derselben Anfrage pausiert der Server ein Video, das gerade in IHR Panel spielt — Musik läuft weiter, JB 24.09.; ein Jellyfin-Film meldet dabei seine Stelle an Jellyfin, ohne „gesehen“, 25.09.) | Start über `SyncYouTube-Fenster.bat`; setzt vor jedem Fenster die Kennung `JBK.SyncYouTube` (`windows_kennung`); ruft `youtube_app.py` als Subprozess; js_api zeigt der Seite NUR Methoden (`video_rect`, `video_melden`) — öffentliche Daten ließen pywebview in das .NET-Geflecht laufen (Hänger 07.08./24.09.); Wächter `tests/test_huelle.py` |
| `youtube_app.py` | Das Herzstück: Warteschlange + Worker, HTTP-Server (Port 8776), Download-Logik (yt-dlp), „Schon-geladen"-DB, Bibliothek, Playlists, Abos, Tray, `main()`. Löschen nie endgültig (25.09.2026, Gesamtprüfung S5/S6): Papierkorb, sonst (auf Wechseldatenträgern und Netzlaufwerken gleich) Ordner `_Papierkorb` im Download-Ordner (gemeldet in `yt_fehler.jsonl`); Spiegel-Sync verschiebt nach `_entfernt` im Ziel, Merkblatt `sync_kopien` je Zielordner. Beide Ordner sind von Index, Einsortieren, Import und Suche ausgenommen. Vertrauen in Anfragen (25.09.2026, Gesamtprüfung Gruppe 3): vor jedem Riegel prüft der Handler den Host-Kopf (IP-Literal, localhost oder eigener Rechnername) und den Origin-Kopf (fehlt, eigener Ursprung oder Browser-Erweiterung; keine Content-Type-Pflicht, das Addon sendet ohne); jede Antwort trägt `frame-ancestors 'self' http://127.0.0.1:8765` (Dashboard bettet `/?embed=1` ein). Fernsteuerung aus sperrt Nicht-Lokale sofort (auch gekoppelte Geräte), Versuchsbremse je IP für Code und Geräte-Token (ab 10 Fehlversuchen 1 s, verdoppelnd bis 15 min; `_bremse_versuch` prüft und belegt in einem Schritt, gleichzeitig laufen höchstens so viele Vergleiche, wie bis zur nächsten Sperre frei sind, der Rest wartet kurz), `timeout` 30 s für das Lesen einer Anfrage (die Strom-Wege heben es nach dem Kopf auf, ein pausierter Film bricht nicht ab), POST-Körper höchstens 2 MB, abgewiesene POSTs schließen die Verbindung. `_id_tag_lesen` nimmt nur plausible Ids; `auffaellige_schluessel()` meldet Schlüssel mit Sonderzeichen beim Start, ändert nichts | importiert `geo`, `oberflaeche`, `handy`, `windows_kennung`, `cookie_kopie`; jeder YoutubeDL entsteht in `_ydl` (Wächter `tests/test_cookies_wal.py`); **Einbahn-Regel:** importiert NIE `vpn` direkt |
| `filme.py` | Film-Fundament (`Doku/SYNC_FILME_SPEC.md`, JB-Go 05.08.): Jellyfin-Katalog (Renés Server) als lokaler Spiegel, Bild-Cache, TMDB/OMDb-Anreicherung, Reihen-Engine, Stream-URL für den VLC-Motor, Fortschritts-Queue; Zugangsdaten NUR im Keyring, Token verlässt den Server nie | von `youtube_app` gerufen (`/api/filme/*`, 6-h-Haken in `ticker_schleife`); importiert NIE zurück (Einbahn-Regel wie geo/vpn) |
| `profil_geraete.py` | Teilprojekt 3 (JB-Go 05.08.): Profile („Wer schaut?") + Geräte-Registry mit Pairing-Code-Fluss und je-Gerät-Token (einzeln widerrufbar, an Profil gebunden); `geraet_ok()` = Riegel-Kern für alle Nicht-localhost-Zugriffe (Token und Pairing-Code zeitkonstant verglichen, `hmac.compare_digest`, 25.09.2026); jeder Schreiber geht durch `_aendern` (Lesen-Ändern-Schreiben unter `familie.json_aendern` plus Modul-`RLock`, S8), Prüfen und Auflisten schreiben nie, eine unlesbare `profile.json` wird nie überschrieben; Pairing-Seite (`PAIRING_HTML`). Bewusst nicht `profile.py` (stdlib-Schatten) | von `youtube_app` gerufen (`/api/geraet_*`, `/api/profile`, `/koppeln`, `_hat_zugriff`); importiert NIE zurück |
| `live_tv.py` | 📡 Live-TV (JB-Go 05.08.): lädt + parst die legale kodinerds-clean-M3U (öffentlich-rechtliche m3u8), 24-h-Cache; VLC spielt die Streams direkt | von `youtube_app` gerufen (`/api/live`, `/api/live/play`); importiert NIE zurück |
| `geo.py` | Gestufte Geo-Umgehung (Header-Trick → eigene Proxys → Gratis-Proxys → VPN-Adapter); erkennt und parst Geo-Fehler **provider-unabhängig** | von `youtube_app` gerufen; ruft `vpn` |
| `vpn.py` | Ausschließlich NordVPN-Steuerung (CLI + Insights-Status) — NordVPN-Interna leben nur hier | nur von `geo.py` gerufen (Einbahn-Abhängigkeit seit 09.07.2026) |
| `oberflaeche.py` | Die gesamte PC-Oberfläche als ein HTML/CSS/JS-String; wird bei jedem Seitenaufruf heiß nachgeladen (`importlib.reload`) | von `youtube_app` ausgeliefert; enthält den `LAYOUT_KERN`-Inline-Block (Wächter `test_layout_kern` im Familien-Repo erzwingt Gleichheit mit `SyncDashTray/System/layout_kern.js`) |
| `handy.py` | Schlanke Touch-Oberfläche der Handy-Fernsteuerung (Route `/m`, opt-in mit Code); seit 23.09.2026 Sperrbildschirm-Steuerung (Titel/Cover/Vor/Zurück), solange das Handy selbst spielt. Der Titel-Schlüssel geht nur als `data-key` in den Handler, `esc` maskiert auch Anführungszeichen (25.09.2026, S3) | von `youtube_app` ausgeliefert; setzt `medien_session.MEDIEN_JS` ein |
| `medien_session.py` | Gemeinsamer Media-Session-Baustein (23.09.2026) für PC-Oberfläche und Handy-Seite: Knöpfe an-/abmelden, Metadaten, Zustand + Zeitleiste (geklemmt, Strom = gelöscht), `freigeben()` für abgelöste Medienelemente. Weiß NICHT, was „Weiter" bedeutet — das entscheidet die Seite | per Platzhalter `/*MEDIEN_SESSION_JS*/` in `oberflaeche.HTML` und `handy.HTML` eingesetzt (Rohvorlage `_HTML_ROH`); heiß nachgeladen vor beiden Seiten und im `ui_stand` mitgezählt; Wächter `tests/test_medientasten_verhalten.py` + `tests/test_medientasten_randfaelle.py` (führen das JS mit deno aus) |
| `medien_smtc.py` | Windows-Anmeldung des VLC-Motors (23.09.2026, JB-Go „pywinrt im Server"): verstecktes Fenster + `SystemMediaTransportControls` — Titel/Cover/Zeitleiste im Windows-Overlay, Knöpfe/Medientasten steuern VLC; Weiter/Zurück reicht es als `taste`-Zähler an die Seite | von `youtube_app` gerufen (`/api/vlc`: `cmd:"medien"`, Felder `smtc`/`taste` in jeder Antwort); Pakete `winrt-*` 3.2.1; fehlt winrt, läuft alles ohne Anmeldung weiter; Wächter `tests/test_medien_smtc.py` |
| `windows_kennung.py` | Windows-Kennung (JB 24.09.2026: „Kennung + Startmenü-Eintrag"): `setze_kennung()` meldet den Prozess als `JBK.SyncYouTube` — ohne sie ordnete Windows den pythonw-Server der Verknüpfung „IDLE (Python 3.14)" zu (Befund 23.09.); `startmenue_eintrag()` legt die Verknüpfung „SyncYouTube" im Startmenü des Nutzers mit derselben Kennung an bzw. zieht sie nach (Ziel wie `SyncYouTube.bat` bzw. die exe; EINE stabile Regel seit 25.09.: das Ziel eines eigenen Eintrags wechselt nur, wenn das bisherige nicht mehr startet — sonst „behalten“; idempotent; eine fremde Verknüpfung gleichen Namens ohne diese Kennung bleibt unberührt; Schreiben über Zwischendatei + `os.replace`). Nur ctypes-COM (IShellLinkW/IPersistFile/IPropertyStore), kein pywin32 | `youtube_app.main()` ganz vorn (`setze_kennung`) und hinter dem Einzel-Instanz-Riegel im Hintergrundfaden (`startmenue_eintrag`, nie im Testmodus); `huelle.main()` vor jedem Fenster (`setze_kennung`); exe über `hiddenimports` der spec; Wächter `tests/test_windows_kennung.py` (schreibt nur in tmp_path, liest per PowerShell zurück); die Startmenü-Wache steht in `tests/conftest.py` |
| `cookie_kopie.py` | Firefox-Cookies samt WAL für yt-dlp (24.09.2026, Lehre aus SyncFindus `fremde_datenbank.lese_kopie`): yt-dlp 2026.08.19 kopiert nur `cookies.sqlite`, nicht das `-wal` — frische Anmelde-Cookies fehlten. `firefox_profil(auswahl)` kopiert Hauptdatei + `-wal` NUR als Datei (Firefox darf laufen, auch mit exklusiver Sperre; WAL-Kopf-Vergleich gegen einen Neubeginn), zieht per Backup-API EINE vollständige `cookies.sqlite` (Rollback-Modus) in einen frischen Temp-Ordner und gibt dessen Pfad als Profil; danach ist der Ordner weg. Jeder Fehler ⇒ None ⇒ der heutige Weg. Liest nie Cookie-Werte | nur von `youtube_app._ydl` gerufen (bei `cookiesfrombrowser == ("firefox",)`); `WURZELN` injizierbar — `tests/conftest.py` setzt es in JEDEM Test auf einen Ort ohne Profil (nie JBs echtes Firefox-Profil); exe über `hiddenimports` der spec; Wächter `tests/test_cookies_wal.py` (echte SQLite mit WAL in tmp_path, echter yt-dlp-Extraktor, kein Netz) |
| `update.py` | Selbst-Aktualisierung: prüft `releases/latest`, lädt die neue Fassung, tauscht sie aus. **Vorgabe seit 08.09.2026: AN** (`STANDARD_CONFIG["auto_update"]`); der Tausch läuft nur im Leerlauf, weil er den Prozess hart beendet | von `youtube_app._update_hintergrund` täglich gerufen; Wächter `tests/test_vorgaben.py`; **MERKE:** Setup-Installationen nie auf die onefile-Form zurückfallen lassen, und die Signatur der geladenen exe wird NICHT geprüft (nur Größe + SHA256) |
| `tools/medien_probe.py` | Werkstatt (23.09.2026): misst Medientasten und Windows-Medien-Overlay über Windows' EIGENE Sitzungsliste (GSMTC per PowerShell-WinRT) — welche App ist „aktuell" (bekommt die Tasten), Titel/Interpret/Album/Zeitleiste/Knöpfe; drückt Medientasten per SendInput (nur mit `--nur-wenn`, wenn die eigene Sitzung aktuell ist) und Overlay-Knöpfe gezielt an EINE Sitzung | von Hand bei Prüfungen; nie im Auslieferungsweg. Vorsicht: Spotify pausiert bei Play/Pause mit, solange es spielt |
| `build_release.py` | Werkstatt (08.09.2026): EIN Weg für die exe — PyInstaller (`SyncYouTube.spec`), seit 24.09. mit fail-closed Erzeugnis-Prüfung (stehen die winrt-Erweiterungen aus den Importen von `medien_smtc.py` und `winrt/msvcp140.dll` in `build/SyncYouTube/PKG-00.toc`?), dann signieren, Prüfsumme, nach oben kopieren | von Hand vor einem Release; ruft `SyncDashTray/System/signieren.py`. Wächter: `tests/test_build_release.py` |
| `tools/quellstart_paket.py` | Werkstatt: baut `SyncYouTube-Quellstart.zip` (signiertes python.org-Embeddable in der Fassung des Bau-Pythons + gepinnte Pakete samt pywinrt + Laufzeit-Dateien ohne Werkstatt + `bin/` + LICENSE/LIZENZEN.md/README.md) für Smart-App-Control-Nutzer (Pete-Fall 07.08.; Umbau 24.09., weil das v.1.2.6-ZIP nicht lauffähig war). Baut in einem frischen Ordner, Pflicht-Rauchtest vor dem Zippen | von Hand vor einem Release; Ergebnis nach `dist_exe/`, mit `--ausgabe ORDNER` woandershin (Probebau). Wächter: `tests/test_quellstart_paket.py` |
| `fernbedienung.py` | Fake-Fernbedienung unter `/fernbedienung`: kleines Fenster mit D-Pad, sendet Tastennamen per BroadcastChannel an den Fernsehmodus (JB-Wunsch 07.08.) | `youtube_app.py` (Route, heiß nachgeladen); Gegenstück in `oberflaeche.py`: `fbEmpfangVerkabeln`/`fernbedienungOeffnen` |
| `familie.py` | gemeinsamer Kern der Familie (Pfade/json/status) — byte-gleiche Kopie | Wächter `test_familie_kern` im Familien-Repo erzwingt Gleichheit; Verteilung über `vendor_kern.py` |
| `layout_kern.js` | gemeinsamer Layout-Mathe-Kern (Snapping/Überlappung/Viewport-Einpassung, DOM-frei) — vendorte Kopie | Master liegt in `SyncDashTray/System/layout_kern.js` |

## Unterordner

| Ordner | Zweck |
|---|---|
| `tests/` | Verhaltens-Sicherheitsnetz (reine Funktionen, kein Netz/keine Platte). Start: `python tests/test_youtube.py` — läuft auch ohne pytest. `conftest.py` (24./25.09.2026): Wachen für JEDEN Test — die Firefox-Profilsuche von `cookie_kopie` UND yt-dlps eigene (`_firefox_browser_dirs`) zeigen auf einen Ort ohne Profil; `windows_kennung.startmenue_ordner` ist gesperrt und schlägt laut an (auch im Hintergrundfaden, Meldung am Testende); `youtube_app` wird früh importiert (sein `filme.einrichten(DATEN_DIR)` beim Import lief sonst nach dem `einrichten(tmp_path)` eines Tests). Daten-Wache (25.09.2026, Gesamtprüfung Y0): alle Datenpfade der App (am Wert gefunden, nur `SCRIPT_DIR`/`BIN_DIR` bleiben), `profil_geraete` und `live_tv` liegen je Test in `tmp_path`, die beim Import geladenen Zustände (Q, CFG, Bibliothek, Playlists, Abos …) kommen je Test frisch; ein Audit-Hook lässt jedes Schreiben in den Programmordner laut scheitern, dazu Netzsperren für `live_tv`, `geo`, `vpn` und `update`. Der Direkt-Lauf ohne pytest hat diese Wache nicht und fährt nur die Tests vor seinem Runner-Block mitten in `test_youtube.py` (25.09.2026: 59 von 213; vollständig nur `python -m pytest tests`). Geprüft in `test_wachen.py`. `test_oberflaeche_syntax.py` (Gesamtprüfung O0): alle `<script>`-Blöcke der Oberfläche einzeln und zusammen per deno übersetzt, nie ausgeführt (fängt Syntaxfehler und doppelte `let`/`const` über Blöcke hinweg); fehlt deno, sichtbar übersprungen. `test_loeschen_und_pfade.py` (Gesamtprüfung Gruppe 2): Abo-Index, Cover, Untertitel, Lösch-Rückfall, Playlist-Sync, Live-TV und WireGuard mit `..\x`, absolutem, UNC- und gültigem Wert; fängt jeden Datei-Zugriff auf UNC-Pfade ab, bevor Windows ihn sieht `test_zugang_und_vertrauen.py` (Gesamtprüfung Gruppe 3): echter Handler samt echtem Riegel (ohne Ersatz) mit LAN-Adresse, Code, Geräte-Token, fremdem Host, fremdem Origin und Addon-Ursprung; Versuchsbremse mit steuerbarer Uhr und parallelen Stößen hinter einer Schranke; am echten ThreadingHTTPServer auf 127.0.0.1: Zeitlimit beim Lesen, Strom mit Pause über alle drei Strom-Wege, Keep-Alive-Nachbau und eine im Körper eingeschmuggelte zweite Anfrage. `test_schluessel_im_handler.py` (S3): rendert Kachel, Listenzeile, Dubletten, Eigenschaften, Transkript-Suche und die Handy-Liste per deno mit einem Schlüssel aus ' " < und prüft das HTML mit einem Parser. `test_oberflaeche_laden.py` (F9): `laden()`/`configFuellen()` als echtes Seiten-JavaScript |
| `browser-addon/` | Universelle Erweiterung für Firefox/Chrome/Edge aus EINEM Code (`build.py`); AMO-signiert, hält sich über `updates.json` selbst aktuell |
| `firefox-addon/` | Firefox-spezifische Altfassung/Signier-Artefakte |
| `abo_index/` | Index-Dateien der Kanal-/Playlist-Abos (Backkatalog, RSS-Puls) |
| `lizenzen/` | Lizenztexte fremder Bestandteile, deren Paket keinen mitbringt (24.09.2026: `pywinrt_LICENSE.txt`, MIT, weil pywinrts dist-info keine LICENSE enthält); geht mit ins Quellstart-Paket, Übersicht in `../LIZENZEN.md` |
| `bin/` | mitgelieferte Werkzeuge — **darin Deno**: yt-dlp braucht seit 2026 eine JS-Runtime für YouTubes n-Challenge, sonst „No video formats found" |
| `build/`, `dist/`, `dist_exe/`, `build_tmp/` | PyInstaller-Bauwerk und Ergebnisse (nicht committen) |

## Daten (nicht committen, enthalten JBs Daten)

`config.json` (Einstellungen: Port, Cookies-Browser, Geo, Unterordner) ·
`warteschlange.json` (offene Downloads, überlebt Neustarts, Resume via `.part`) ·
`geladen_log.json` (die Bibliothek = fertige Downloads) · `playlists.json` ·
`abos.json` · `lyrics_cache.json` (LRCLIB-Karaoke) ·
`yt_status.json` (**read-only fürs Dashboard**: Zähler + letzte Datei)

## Doku daneben

`_ARCHITEKTUR.md` (Schichtungs-Diagramm + Aufräum-Fahrplan) ·
`NAECHSTE_SESSION.md` (Übergabe innerhalb dieses Repos) ·
`SyncYouTube.spec` (PyInstaller)
